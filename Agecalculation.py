# age= input ("How old are you? \n")
# #convert type to int.
# int_age= int(age)
# year_calculation= (2024-int_age)
# #convert agian to string as it is now an integer
# str_year_calculation= str(year_calculation)
# print ("You were born in " + str_year_calculation) 
# #This code could be done in 2 lines only by the following
int_age= int(input ("How old are you? \n" )) #First apply the int function over  the input function as shown.
born_in= print(f"You were born in {2024-int_age}") # apply an f-string, or formatted string literal, is a string literal that is prefixed with 'f' or 'F'. These strings may contain replacement fields, which are expressions delimited by curly braces {}. 

